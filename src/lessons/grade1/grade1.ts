import additionSection from "./additionSection/additionSection";
import subtractionSection from "./subtractionSection/subtractionSection";

let grade1 = {
    id: 1,
    name: 'grade1',
    progress: 0,
    lessonSections: [additionSection, subtractionSection],
}

export default grade1;